# Função lamber usando condicional para verificar se um número é par ou impar
par_impar = lambda x: "par" if x % 2 == 0 else "impar"
print(par_impar(5))  # Saída: Par  
print(par_impar(-2))  # Saída: Par